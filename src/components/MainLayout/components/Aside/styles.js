import styled from 'styled-components';

export const Wrapper = styled.aside`
  width: 300px;
  left: 0;
  top: 0;
  bottom: 0;
  position: fixed;
`;

export const List = styled.nav`
  display: block;
  
  a {
    display: block;
  }
`